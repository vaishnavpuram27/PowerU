import {Button, Modal } from "react-bootstrap";
import {useState} from "react";

function Texta(){

    return(alert("Added to cart"));
    // const [successful,setSucessful]=useState(false);
    
    //   return(<Modal show={successful} onHide={()=>setSucessful(undefined)}>
    //   <Modal.Body>Added to cart</Modal.Body>
    //   <Modal.Footer>
    //       <Button variant="secondary" onClick={()=>setSucessful(undefined)}>Close</Button>
    //   </Modal.Footer>
    //   </Modal>)
  
     
}

export default Texta;